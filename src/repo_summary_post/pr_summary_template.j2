# {{ project_name }}: recent activity (from {{ start_date }} to {{ end_date }})
{% for item in items %}
{% if item.type == 'pull_request' %}
## Pull request #{{ item.number }} updated {{ item.updated_at[:10] }}: {{ item.title }} ({{ item.status }})
{% else %}
## Issue #{{ item.number }} updated {{ item.updated_at[:10] }}: {{ item.title }} ({{ item.status }})
{% endif %}
{%- if item.body %}

{{ item.body | trim | indent(4) }}
{%- endif %}

### RECENT ACTIVITIES for {{ item.type }} #{{ item.number }} BETWEEN {{ start_date }} and {{ end_date }}
{%- for activity in item.recent_activities %}
{%- if activity.type in ['comment', 'commit'] %}

#### {{ item.type | capitalize }} #{{ item.number }} / recent {{ activity.type }} from @{{ activity.author }} on {{ activity.date.strftime('%Y-%m-%d %H:%M:%S') }}

{{ activity.body | trim | indent(4) }}
{%- elif activity.type in ['merge', 'close'] %}

#### {{ item.type | capitalize }} #{{ item.number }} / {{ activity.type }} on {{ activity.date.strftime('%Y-%m-%d %H:%M:%S') }}
{%- endif %}{# activity.type #}
{%- endfor %}{# activity in item.recent_activities #}
{% endfor %}{# item in items #}
